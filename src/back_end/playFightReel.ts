import * as FightReel from '../shared/fightReel';
import { Status } from '../shared/help';

export function playFightReel(
    status: Status[],
    reel: FightReel.Event[],
    playTo: number
) {
    for (let event of reel) {

    }
    return status;
}
